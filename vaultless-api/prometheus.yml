global:
  scrape_interval: 5s  # Keep for testing

scrape_configs:
  - job_name: 'vaultless-api'
    static_configs:
      - targets: ['localhost:8080'] # ['host.docker.internal:8080']
    metrics_path: '/metrics'